<html>
  <body>
    <meta charset="utf-8" />
    <h1>{{ school_name }} keyword watch</h1>
    <p>Configured safeguarding watch terms: {{ watch_terms|join(', ') }}</p>
    <ul>
    {% for incident in incidents %}
      <li>
        <strong>{{ incident.display_name }}</strong>
        — {{ incident.filter_type }} / {{ incident.severity }}
        — {{ incident.timestamp|localtime(school_timezone) }}
      </li>
    {% endfor %}
    </ul>
  </body>
</html>
